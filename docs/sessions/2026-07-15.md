## Context system adopted
- did: initialized docs/ context system; reconstructed STATE.md + old_context.md from existing planning docs
- changed: added CLAUDE.md, merged router into AGENTS.md, added docs/STATE.md, INDEX.md, decisions.md, conventions.md, old_context.md, sessions/, specs/
- next: begin implementation with ticket 01-prove-daemon-cli-lifecycle

## 11:20 — Ticket 01 daemon and CLI lifecycle
- did: built the initial Rust workspace, recorded a failing six-test RED run, implemented versioned Unix IPC and the complete controlled Recording lifecycle, and reached GREEN
- changed: `Cargo.toml`, `Cargo.lock`, `crates/voisu-core/`, `crates/voisu-app/`, `docs/STATE.md`, and `docs/decisions.md`
- decided: package both binaries in `voisu-app`; version IPC in both its XDG socket path and payloads -> `docs/decisions.md`
- next: rerun in an environment with writable `.git`, `rustfmt`, and `clippy` to record the required RED/GREEN commits and finish toolchain verification

## 12:05 — Ticket 01 review findings resolved
- did: extended the public suite RED-first, replaced the blocking lifecycle mutex with an actor, added
  Recording-scoped provider coordination, recoverable capture abort, structured evidence, safe socket
  ownership, typed redaction, envelope-first decoding, and bounded IPC; verified build and all 16 tests GREEN
- changed: `Cargo.lock`, both crate manifests, `voisu-core`, `voisu-daemon`, daemon acceptance tests, and core
  provider-coordination tests
- decided: actor-owned transitions, Recording-scoped dual-provider coordination, and capability-style runtime
  socket ownership -> `docs/decisions.md`
- next: create the requested conventional RED/GREEN commits once `.git` is writable, then proceed to the next
  approved ticket

## 12:47 — Checkpoint: Ticket 01 approved, docs reconciled
- did: confirmed Ticket 01 complete and APPROVED after 3 external review rounds (commits 2331854, d288ef8,
  48f6353, 63c0a97); verified `.git` is writable and all four commits are on `main` with a clean tree;
  recounted the suite at 25 green tests (21 in `daemon_cli_lifecycle.rs` + 4 in `provider_coordination.rs`);
  rewrote `docs/STATE.md` to drop the stale "git read-only" / old test-count claims and reflect current reality
- changed: `docs/STATE.md`, `docs/sessions/2026-07-15.md`, `docs/decisions.md`
- decided: rustfmt/clippy remain unavailable on this machine (need `sudo dnf install rustfmt clippy`) and are
  deferred rather than blocking; codex exec runs must never be deliberately backgrounded -> `docs/decisions.md`
- next: dispatch Ticket 02 (`.scratch/voisu-implementation/issues/02-verify-fedora-readiness.md` — Fedora
  readiness checks + Secret Service credentials)

## 13:03 — Ticket 02 Fedora readiness and credentials
- did: added public `doctor`, stdin-only `auth set`, and independent `auth verify` workflows; exercised
  readiness PASS/WARN/FAIL, Secret Service replacement/fallback, provider-auth outcomes, and credential redaction
  through controlled CLI acceptance tests; verified `cargo build --workspace`, `cargo test --workspace` (30 green),
  and `git diff --check`.
- changed: `crates/voisu-core/src/lib.rs`, `crates/voisu-app/src/bin/voisu.rs`,
  `crates/voisu-app/tests/daemon_cli_lifecycle.rs`, `docs/STATE.md`, and `docs/decisions.md`.
- decided: credentials enter only through standard input, go to desktop Secret Service via `secret-tool`, and use
  explicit provider environment variables only as a non-persistent development/headless fallback -> `docs/decisions.md`.
- next: review and create the requested conventional RED/GREEN commits once `.git` metadata is writable, then dispatch Ticket 03.

## 13:45 — Ticket 02 security review remediation
- did: fixed every reported subprocess, credential, readiness, and reuse finding RED-to-GREEN; the independent
  nested review command was attempted but could not authenticate in its temporary Codex home.
- changed: extracted reusable system adapters into `crates/voisu-app/src/system.rs`, made child environments explicit
  and bounded, hardened curl verification, and added PATH-stubbed public CLI acceptance coverage.
- decided: subprocess allowlists and deadline ownership belong with the reusable adapter module -> `docs/decisions.md`.
- next: commit the focused Ticket 02 remediation, then dispatch Ticket 03.

## 13:46 — Commit blocked by Git metadata sandbox
- did: attempted the requested conventional commit `fix(auth): harden Ticket 02 subprocess boundaries (#2)`.
- changed: no Git commit was created; the verified worktree remains intact.
- next: retry the named commit when `.git` is writable, then dispatch Ticket 03.

## 14:31 — Checkpoint: Ticket 02 approved and closed, Ticket 03 dispatched
- did: confirmed Ticket 02 is COMPLETE and APPROVED after 4 Sol review rounds; verified all 5 commits
  (`feb5472`, `bbc86d3`, `0bc3944`, `acfa7d3`, `cfe336f`) landed on `main`, GitHub issue #2 closed, and
  `main` pushed to origin; recounted the suite at 44 green tests (40 CLI acceptance + 4 provider
  coordination); rewrote `docs/STATE.md` to drop the stale "git read-only"/uncommitted-fix framing and
  reflect the merged, closed state; recorded the standing subprocess-cleanup hardening invariants in
  `docs/decisions.md`.
- changed: `docs/STATE.md`, `docs/sessions/2026-07-15.md`, `docs/decisions.md`, `docs/INDEX.md`.
- decided: Ticket 02 subprocess boundaries (env allowlisting, deadline ownership, bounded joins, response
  caps) are now standing invariants for every future subprocess boundary in this codebase -> `docs/decisions.md`.
- next: Ticket 03 (PipeWire → Groq → clipboard end-to-end, issue #3) is dispatched to codex Sol (medium);
  await delivery, then run the Sol review cycle.

## 14:54 — Ticket 03 PipeWire to Groq clipboard Delivery
- did: implemented the first real Recording-to-Transcript slice RED-to-GREEN: normalized PipeWire capture,
  live bounded/overlapping Groq chunks plus the final audio tail, deterministic chunk Merge Result validation,
  and exactly-once `wl-copy` Delivery; added distinct empty, too-short, silent, and Recording Deadline outcomes,
  provider-start cleanup/recovery, a local Groq server harness, and an ignored opt-in live Fedora smoke test.
- changed: `voisu-core` audio/error contracts; `voisu-app` system adapters, daemon wiring, and CLI response deadlines;
  public daemon/CLI acceptance tests; crate dependency metadata; `docs/STATE.md` and `docs/decisions.md`.
- decided: normalize capture to 16 kHz mono s16 PCM and drain after graceful SIGINT; submit 30-second Groq chunks
  with 500 ms overlap during the Recording and reconcile them deterministically before one Delivery ->
  `docs/decisions.md`.
- verified: `cargo build --workspace`, `cargo test --workspace` (51 passed, one opt-in smoke ignored), and
  `git diff --check`; rustfmt/clippy remain unavailable. The pinned Sol review command was attempted three ways but
  could not initialize because the Codex state directory is read-only.
- next: retry the independent Sol review in a writable environment, commit
  `feat(dictation): deliver PipeWire Recordings through Groq (#3)`, then dispatch Ticket 04.

## 15:33 — Ticket 03 review findings resolved
- did: fixed all seven review findings RED-to-GREEN: automatic pump recovery, responsive blocking startup, shared
  deadline arithmetic, loopback-only plaintext Groq endpoints, bounded subprocess reap/destruction, production-path
  failure coverage, and bounded provider abort; verified 60 tests green with one live smoke ignored.
- changed: `voisu-core` provider lifecycle/coordinator, daemon actor and pump ownership, shared system deadlines and
  subprocess hardening, CLI deadline use, and production CLI acceptance tests.
- decided: every pump termination notifies the actor, provider streams have an explicit bounded abort seam, and the
  public Stop budget is derived from the daemon's capture/provider/Delivery deadlines.
- next: rerun the pinned Sol review when the Codex state directory is writable, then commit
  `fix(dictation): resolve Ticket 03 lifecycle review findings (#3)` and dispatch Ticket 04.

## 16:35 — Checkpoint: Ticket 03 approved and closed
- did: confirmed Ticket 03 (PipeWire → Groq → clipboard end-to-end, issue #3) is COMPLETE and APPROVED after
  5 Sol review rounds, final APPROVE at commit `f51dbbd`; verified all commits through `22a8eb2` are on `main`
  and pushed, issue #3 closed; recounted the suite at 65 green tests (2 system unit + 58 daemon/CLI acceptance
  + 5 provider-coordination), 3x flake-free; rewrote `docs/STATE.md` to describe the new `ActorState::Recovering`
  lifecycle, the `AtomicBool`-based `CancelRegistry` redesign, and the first-review-high/re-review-medium policy.
- changed: `docs/STATE.md`, `docs/sessions/2026-07-15.md`, `docs/decisions.md`.
- decided: recovery is a first-class actor state with retryable rejection (not a deferral queue); subprocess
  cancellation uses an `AtomicBool` flag checked by the handle-owning waiter, never a raw-PID signal; review
  effort policy is first-review-high/re-review-medium -> `docs/decisions.md`.
- next: dispatch Ticket 04 (concurrent Deepgram streaming + Provider Deadline) to codex Sol medium; first
  review of the ticket runs at Sol high.

## 17:06 — Ticket 04 concurrent Deepgram and Provider Deadline
- did: implemented live one-second Deepgram PCM requests alongside Groq, shared Provider Deadline fallback, deterministic
  provider attribution, Deepgram-only validation/Delivery, structured latency evidence, and awaited cancellation cleanup;
  found and fixed a curl-vs-Voisu deadline-classification flake during the required repeated suite runs.
- changed: `crates/voisu-core/src/lib.rs`, `crates/voisu-app/src/system.rs`,
  `crates/voisu-app/src/bin/voisu-daemon.rs`, `crates/voisu-app/tests/daemon_cli_lifecycle.rs`, `docs/STATE.md`,
  `docs/decisions.md`.
- decided: Deepgram uses one-second linear16 HTTP chunks through the existing restricted curl boundary; Voisu's 14-second
  whole-operation budget expires before curl's 15-second internal limit so Voisu owns kill, reap, and Provider Deadline
  classification -> `docs/decisions.md`.
- verified: `cargo build --workspace`, `cargo test --workspace` (71 passed, one live smoke ignored), `git diff --check`,
  and three consecutive `cargo test -p voisu-app` runs (66 passed including unit tests, one ignored per run).
- next: run the first Ticket 04 Sol review at high effort, resolve findings, then commit
  `feat(dictation): stream Deepgram within the Provider Deadline (#4)`.

## 23:07 — Ticket 04 first-review findings resolved
- did: fixed all 2 HIGH and 2 MEDIUM findings RED-to-GREEN: Provider Deadline losers now run bounded awaited abort before
  `Idle`, Deepgram curl ownership is capped at three queued permits, the production unavailable env switch is removed in
  favor of PATH-stubbed boundaries, and non-overlapping Deepgram transcripts use ordered concatenation.
- changed: provider-stream completion ownership and deadline cleanup in `voisu-core`; daemon recovery-budget wiring and
  system-boundary test mode; Deepgram scheduling/merge logic; production acceptance stubs and regressions; project state
  and decision docs.
- decided: queue every Deepgram audio chunk behind a three-permit curl cap and await handles in creation order; do not
  coalesce chunks or apply Groq's overlap-removal merge to non-overlapping Deepgram audio -> `docs/decisions.md`.
- verified: `cargo build --workspace`, `cargo test --workspace` (75 passed, one live smoke ignored), `git diff --check`,
  and three consecutive `cargo test -p voisu-app` runs (69 passed, one ignored per run).
- next: run the Ticket 04 Sol re-review at medium effort, resolve any remaining findings, then commit
  `fix(dictation): resolve Ticket 04 review findings (#4)`.

## Checkpoint — mid-Ticket-04 review loop
- did: checkpointed the repo mid-Ticket-04. Confirmed `3e4eecc` (feature, 71 tests) is the last committed/verified
  state. Confirmed the first review (Sol HIGH) returned REQUEST-CHANGES: 2 HIGH (Provider Deadline cancellation not
  awaited before Idle; no in-flight cap on Deepgram curls) + 2 MEDIUM (`VOISU_TEST_DEEPGRAM_UNAVAILABLE` production
  bypass; wrong overlap-removal merge for non-overlapping Deepgram chunks). Confirmed Sol's medium fix round left
  UNCOMMITTED worktree changes claiming all 4 fixed (75 tests), but
  `provider_deadline_kills_and_reaps_late_deepgram_curl_before_idle`
  (`crates/voisu-app/tests/daemon_cli_lifecycle.rs:2047`) fails deterministically (0.14s) on the orchestrator's
  machine; 65 other acceptance tests pass. A further Sol medium feedback round was in flight (background codex run)
  fixing that root cause and may modify the worktree further.
- changed: `docs/STATE.md`, `docs/sessions/2026-07-15.md` only — no code was touched or committed; the failing-test
  Rust changes remain uncommitted in the worktree exactly as left by the in-flight fix round.
- decided: nothing new load-bearing; carried forward the existing first-review-high/re-review-medium policy.
- next: for the next agent — wait for/verify the in-flight fix (full `cargo test --workspace` green + 3x
  flake-free `cargo test -p voisu-app`), commit as `fix(dictation): resolve Ticket 04 review findings (#4)`,
  dispatch a Sol MEDIUM re-review, iterate to APPROVE, close issue #4, push, update
  `docs/model-benchmark.md` rows for the ticket 04 fix rounds, then checkpoint. If Sol fails again, escalate to an
  Opus subagent (high effort) per the fallback ladder.
