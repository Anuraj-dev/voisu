## 08:45 — Codebase audit + hardening wayfinder map charted
- did: full read-only audit (4 parallel Sonnet passes + driver verification of
  both criticals; 297 tests green). Scores: types 8, modularity 6, security 8.5,
  quality 7, testing 7. Charted `.scratch/voisu-hardening/` map: audit report
  asset + 5 tickets + trigger-ruled fog (system.rs split, provider
  collectionization, CI-only e2e harness).
- changed: docs/STATE.md (priority block already committed as 1a9b6c7 by the
  concurrent session; this checkpoint adds audit status + map pointers),
  .scratch/voisu-hardening/* (new), docs/INDEX.md, docs/decisions.md.
- decided: criticals before latency; cheap items parallel-safe; minors bundled;
  refactors + e2e on trigger rules (-> decisions.md).
- next: RPM + live WER gate (unchanged); after accuracy integrates -> hardening
  tickets 01+02, then latency. 03/04 takeable anytime.

## 09:42 — Live-gate bugs fixed, RPM shipped locally, WER scored, stress test PASS
- did: root-caused & fixed two live-blocking daemon bugs — (1) missing rustls
  CryptoProvider panicked every Deepgram connect; (2) recordings SIGKILLed at
  ~10 s because PR_SET_PDEATHSIG fires on forking-THREAD exit and pw-record was
  spawned from a reaped Tokio blocking-pool thread. Fixed two %check test races,
  RPM license compliance (ring: MIT AND Apache-2.0 AND ISC). Sol high review →
  2 findings fixed → Sol medium APPROVE. RPM git2f90a10 built (%check green,
  298 tests), installed, live-tested: 4-paragraph WER = 10.8% raw / 10.0%
  adjusted (baseline 26.3%); both providers on all recordings; post-finalize
  1.0–1.5 s; 118 s continuous stress recording PASS.
- changed: system.rs (spawn-from-reader-thread handoff, install_crypto_provider),
  voisu-daemon.rs (explicit runtime + keep-alive test seam), Cargo.toml (rustls/
  ring), packaging/voisu.spec (license), daemon_cli_lifecycle.rs + service_cli.rs
  (races + reap regression test), examples/deepgram_probe.rs (new),
  docs/model-benchmark.md rows 84–86. Commits a0899b3..2f90a10.
- decided: pdeathsig only from long-lived threads; ring over aws-lc-rs
  (-> decisions.md). Gate is marginal → NOT pushed; ship/hold is Raja's call.
- next: Raja decides ship-as-is vs one more CLI-jargon keyterm pass; then push
  branch, PR, merge on CI green (no AI credits); then hardening criticals 01+02.

## 10:58 — CLI keyterm pass, retest, flake fix, MERGED to main
- did: dictionary keyterm pass (f027a2b: prompt-budget truncation root cause;
  distinctive CLI terms reordered/added + truncation-guard test); RPM
  git44810d1 built+installed; live retest scored 10.8% raw / 9.2% adjusted —
  all CLI compounds correct; Deepgram verdict = keep (reconciliation
  15.1%→10.9% avg). Pushed branch, opened PR #23; CI flake-gate caught a real
  test race (socket-exists ≠ IPC-ready in manual-daemon service tests) —
  fixed ba83650; CI green; MERGED as 524deda. Local main synced.
- changed: dictionary.rs, tests/service_cli.rs, docs (benchmark rows 87,
  STATE, sessions).
- decided: keep Deepgram (evidence above); generic English words stay OUT of
  the built-in dictionary to protect the 224-byte Whisper prompt budget.
- next: delivery bug (xkbcommon/libei keymap fd) — orchestrator handoff prompt
  delivered to Raja; check fix/delivery-keymap-fd worktree first.

## 11:18 — Delivery keymap fd bug fixed, live-verified, merged (PR #24)
- did: adopted the prior session's staged-uncommitted pread fix from the /tmp
  worktree (judged complete, not stale), committed as `3b88341`, rebased clean
  onto post-#23 main, removed the worktree. Regression test GREEN; workspace
  300/300. Sol high first review: APPROVE, zero findings (~112k tokens,
  benchmark row 88). RPM `gitfd3c663` built (clean tree, %check pass), Raja
  installed and live-verified: `delivery_method: compositor_submitted` on live
  dictations — clipboard Ctrl-V era over. Pushed, PR #24, CI green
  (test gate + 3x flake), merged `a570e97`.
- changed: crates/voisu-app/src/system.rs (read_keymap_fd + test),
  CLAUDE.md/AGENTS.md (delegation section deleted), docs/model-benchmark.md
- decided: pread-at-offset-0 for compositor-owned fds (-> decisions.md);
  Raja's order — never instruct codex to delegate to claude -p workers (rule
  deleted from repo + global agent docs, saved to driver memory)
- next: the two critical audit fixes (process_recording supervision,
  stop_child spawn_blocking) — see STATE.md

## 13:14 — Both hardening criticals shipped (C1 + C2) via cladex
- did: C1 merged (PR #25, `da279b3`) — supervise_recording mirrors
  supervise_replay; 4 review rounds hardened poisoned-lock recovery, honest
  panic diagnostics, rollback-safe persistence, panic-free logging. C2 merged
  (PR #26, `c940d65`) — stop_child on spawn_blocking + full reaper-adoption
  cancellation safety (incl. pre-stop Drop path), 3 review rounds. Tests
  300 → 307. Benchmark rows 89–102 logged. Live RPM gate skipped (failure-path
  changes only; happy path pinned by regression + 3x CI gate).
- changed: voisu-daemon.rs, system.rs, voisu-core lib.rs + diagnostics.rs,
  daemon_cli_lifecycle.rs, docs/model-benchmark.md
- decided: cladex adopted for Sol dispatches (--effort passes through,
  wire-verified); ~50/50 Claude/Codex split (Raja); guaranteed-completion
  supervisor paths must be panic-free; reaper drain_to_completion gates Idle
  (-> decisions.md)
- next: latency effort (.scratch/voisu-latency/) — prune its stale "delivery
  fix" item first; hardening 03/04 parallel-safe anytime

## 15:18 — Latency effort AFK tickets shipped (L-01 toggle + L-04 FLAC); Sol/Opus head-to-head
- did: pruned the shipped delivery fix (PR #24) from the latency spec/map/
  ticket 05 (closed). L-01 Deepgram default-OFF toggle merged (PR #27) — Opus
  implemented, Sol reviewed 3 rounds (8 findings, all fixed RED-first), tests
  307→328. L-04 FLAC upload merged (PR #28) — Sol implemented, DOUBLE
  independent review (Sol high + Opus high, both APPROVE), tests →330. One CI
  failure: FLAC sample-count assertions raced post-signal trap bytes — driver
  root-caused and pinned to the deterministic pre-stop bound. Benchmark rows
  103–113 + head-to-head verdict + routing update committed.
- changed: voisu-daemon.rs, voisu.rs, config.rs (new), system.rs,
  daemon_cli_lifecycle.rs, Cargo.toml/lock (flacenc), latency spec/map/tickets,
  docs/model-benchmark.md, docs/INDEX.md
- decided: disabled Provider = adapter stand-in not coordinator surgery; FLAC
  with no duration gate; test assertions pin only pre-stop capture; subagent
  doc-fence required in every dispatch prompt (-> decisions.md)
- next: HITL with Raja — ticket 02 live Groq-only eval, ticket 03 Deepgram
  fate decision, then the single end-of-effort RPM ship gate with live latency
  + WER evidence
