# Sessions — 2026-07-19

## 01:35 — Fix batch closed + friends map phase A 7/8 (overnight orchestration run)
- did: (spanning 2026-07-18 evening → 01:30) merged the whole fix batch — Deepgram default ON
  (PR #48), hardening-05 sweep (PR #49, 2 rounds), keyterm cap (PR #51), service-ready deadline
  split (PR #52), overlay unavailable-capsule fix (PR #54, resolved the external finding) — then
  map tickets: 01 ADR 0007 (PR #53), 02 delivery_mode (PR #55), 03 focus research (asset),
  04 guarded delivery (PR #56, Sol impl + Opus review caught the callDBus INT32 marshaling trap;
  rebased over 05/08, driver resolved the conflict), 05 dictionary CLI + hot-reload (PR #57,
  last Terra dispatch; Opus fix round added flock serialization), 08 GNOME plain-window fallback
  (PR #59, Opus impl, 2 review rounds → poll_tick seam). Repo made public per Raja (CI billing).
  Filed CI flake as GH #58 (4 hits, passes on rerun). Freed ~3.7 GB disk (incremental cache, wavs).
- changed: config.rs, dictionary.rs, focus.rs (new), system.rs, voisu-daemon.rs, voisu.rs,
  overlay.rs, voisu-overlay.rs, voisu-core lib + tests, daemon_cli_lifecycle/delivery/dictionary_cli
  tests, CLAUDE.md (routing), docs/model-benchmark.md rows 135–156.
- decided: Codex = reviews only, Claude implements (Raja, quota) → decisions.md; guarded-delivery
  wire/staleness/strictness decisions → decisions.md; cladex final-message-only output rule → memory.
- next: ticket 06 (keyring probe) after Raja reboots; then 07 (setup wizard); then phase B
  (09 = Raja's accounts checklist first). HITL queue lives in STATE.md.
