# Sessions — 2026-07-20

## 01:05 — Phase A closed: ticket 06 probe, Trigger Key self-heal (#60/PR61), setup wizard (07/PR62)
- did: (session spanned 2026-07-19 evening → 2026-07-20 early AM, Raja AFK for exam most of it)
  Ticket 06 closed via live Sonnet probe (ksecretd reachable+unlocked at service start; asset + next-reboot
  probe kit). Raja reported mid-session that the Trigger Key dies on every reboot/suspend → filed GH #60,
  root-caused (bind-failure path returned permanently; connection death misread as revocation), fixed via
  PR #61 (Opus impl → 3 Sol review rounds → Fable escalation for rounds 2–3; merged). Ticket 07 shipped via
  PR #62 (Opus impl → 5 review rounds: dead Sol dispatch → Fable review r1, Sol re-reviews r2–r5, Fable fix
  rounds after the 2-round escalation; merged). Phase A of the friends map complete. Benchmark rows 157–172.
- changed: voisu-daemon.rs (shortcut_listener self-heal), system.rs (portal permanence, SecretToolStore),
  voisu-core lib.rs (ShortcutEvent::SessionClosed, KeyDiagnosis, ProviderKeyStatus), NEW setup.rs +
  secret_file.rs, voisu.rs (setup/doctor), config.rs, daemon_cli_lifecycle.rs (+~40 tests, restartable-bus
  + mock-portal harness extensions), provider_key_status.rs; map.md + issues 06/07 closures.
- decided: Trigger Key permanence = portal-refusal only (response 1); no keyring crate (secret-tool
  boundary, zero new deps); fallback-file prune honesty keyed on target-line content; Sol-fails ladder
  updated by Raja to retry-once-then-Fable (memory + ledger updated).
- next: ticket 09 (GH #41, Raja's guided accounts checklist) in a FRESH Fable-medium session with Raja
  live — handoff prompt delivered this session. Then phase B packaging (ticket 10 cargo-deb unblocked).

## 02:15 — Ticket 09 closed: packaging accounts + keys (live HITL)
- did: guided Raja through all 4 areas — GPG apt-signing key (ed25519, passphrased, offline backup on
  Raja SSD verified, pubkey exported to packaging/apt/voisu-archive-keyring.asc); FAS + COPR
  anuraj-dev/voisu (ID 246563, F43/44 x86_64; caught+fixed capital-V case-sensitivity trap); AUR
  anuraj-dev (dedicated no-passphrase deploy key, SSH auth verified live, voisu/voisu-bin free; pacman
  captcha solved via throwaway podman arch container); 4 GH secrets set (AUR_SSH_PRIVATE_KEY,
  GPG_PRIVATE_KEY, GPG_PASSPHRASE, COPR_WEBHOOK_URL), all piped, never echoed. Fact-checked: AUR 2FA
  doesn't exist upstream (proposed post-Atomic-Arch, unshipped) → compensating controls + queued HITL.
  Cloudsmith token skipped (ticket 13 decides Pages vs Cloudsmith in-ticket).
- changed: .scratch/voisu-friends/issues/09-*.md (closed + resolution), map.md decision line,
  docs/model-benchmark.md row 173, packaging/apt/voisu-archive-keyring.asc (new, uncommitted),
  ~/.ssh/config + ~/.ssh/keys/aur_voisu (local), GH #41 closed.
- decided: credentials architecture (GPG passphrased/loopback-CI, AUR key passphrase-free scope-limited;
  values never recorded) -> decisions.md
- next: ticket 10 — cargo-deb package (GH #42), unblocked; commit the checkpoint + keyring .asc.

## 12:57 — Phase B: ticket 10 merged, ticket 11 through first review
- did: Orchestrated ticket 10 (cargo-deb) end-to-end — Opus implemented, Sol reviewed 4 rounds, MERGED
  as PR #63 (c229ff4). Sol's rounds caught real defects each time: `$auto`/shlibdeps dependency
  discovery missing, non-monotonic Debian versions, a bypassable `rm -rf` guard, phantom license
  attributions. Implementer was discarded after 2 failed rounds (escalation rule); driver took round 3
  inline; Sol APPROVED round 4. Then implemented ticket 11 (AUR) via a fresh Opus agent → PR #64,
  Sol first review returned 9 findings, all fixed and pushed (a7d2f6d). Re-review not yet run.
- changed: crates/voisu-app/Cargo.toml ([package.metadata.deb]), packaging/build-deb.sh, packaging/deb/*,
  packaging/aur/voisu{,-bin}/* (PKGBUILD, .SRCINFO, .install, ring license tree), .gitignore,
  docs/model-benchmark.md (rows 174-177).
- decided: deb must be built on Ubuntu (detect-and-fail rather than a hidden container build);
  Debian dev-version ordering keyed on commit count, not committer timestamp -> decisions.md.
- next: Sol re-review of PR #64 at medium effort, merge on CI green, then ticket 12 (COPR).
  Also file the deb+rpm third-party-license-tree follow-up ticket (see STATE.md).

## 17:50 — Ticket 12 (COPR) round 0 + review round 1 in flight
- did: Ticket 11 closed (Sol re-review zero findings; PR #64 merged f31fe8d; benchmark rows 178-179).
  Filed ticket 16 (deb+rpm ring license trees, GH #65). Dispatched ticket 12 → PR #66: vendored
  offline SRPM (cargo vendor as Source1, %global-baked Release), packaging/copr/make-srpm.sh
  custom-source script, copr-trigger.yml webhook workflow, spec voisu_release macro. Sol first
  review (high): 15 findings (4 major — tag-pinning, Release monotonicity, %check env, hardcoded
  version). Fix round dispatched with orchestrator direction; first fix agent killed by Raja
  mid-run (its main commit 8297d04 survived locally, unpushed); replacement Opus agent now
  verifying/finishing/pushing.
- changed: packaging/build-srpm.sh, packaging/copr/, .github/workflows/copr-trigger.yml,
  packaging/voisu.spec (all on feat/copr-channel, unmerged); docs/model-benchmark.md (178-179);
  .scratch/voisu-friends/issues/16-license-tree-deb-rpm.md (new).
- decided: unified RPM Release policy (pre-release 0.<count>.<ct>.git<sha>, tagged plain N) and
  tag self-pinning in make-srpm.sh without a COPR API token -> decisions.md.
- next: when the fix agent pushes → Sol re-review (medium) → Raja merges PR #66 → COPR HITL steps
  (custom-source config, webhook-rebuild flag) → ticket 13 (apt repo).

## 20:31 — Ticket 12 (COPR) merged after 3 review rounds
- did: PR #66 through fix rounds to merge (main f69039b). Round 1: killed agent's commit 8297d04
  covered all 15 Sol findings; takeover Opus verified end-to-end, zero code changes. Round 2 (Sol
  medium): 3 new majors — driver took them inline per escalation rule (HEAD-independent highest-tag
  self-pinning, fail-closed fetches, realpath -m vs -sm symlink-proof confinement, whole-string
  rpm-release anchor). Round 3: cladex died twice → Opus high reviewed → APPROVE (1 minor + 4 nits;
  minor + prune-tags nit applied). Filed GH #67 (flake root cause: missing wait_for_marker).
  Benchmark rows 180-182.
- changed: packaging/rpm-lib.sh, packaging/copr/make-srpm.sh, packaging/build-srpm.sh,
  packaging/build-rpm.sh, packaging/voisu.spec, packaging/rpm-release, .github/workflows/copr-trigger.yml
  (all merged via PR #66); docs (STATE, benchmark 180-182, decisions, sessions).
- decided: COPR channel builds ONLY the highest v<semver> tag once tags exist (snapshots =
  pre-first-release bootstrap or local scripts) -> decisions.md.
- next: ticket 13 (apt repo, GH #45) — read .scratch/voisu-friends/issues/13-apt-repo-channel.md,
  dispatch Opus 4.8 high, Sol review. Then 14 (release workflow), 15 (HITL validation), 16 (licenses).
