## 08:35 — Ticket 13 shipped + secret-lookup fix (session spanned 2026-07-20 evening → limit reset)
- did: Ticket 13 (apt repo, GH #45) delivered and MERGED as PR #68 after 5 review rounds (Sol high/med
  ×3 within budget, Opus high ×2, driver inline for rounds 3–5 after the discard rule fired). Raja's
  installed-build failure report triaged from live journals (Sonnet, row 188): 3 stale signatures,
  PR #61 fix missing from installed RPM, quality-gate drops by design, mid-session secret denial
  genuinely unfixed → filed GH #69 → Opus implemented PR #70 (stderr-classified retry + 300s TTL
  session cache, 439/0) → Opus r1 APPROVE (0 major), CI green, awaiting Raja's merge. Sol budget
  capped by Raja (2 → spent #68 re-r2, granted +1, spent #68 re-r4) → 1 left, reserved ticket 14.
- changed: packaging/apt/* (via PR #68), crates/voisu-app/src/system.rs + tests + docs/decisions.md
  (via PR #70, unmerged), CLAUDE.md (Sol budget cap), docs/model-benchmark.md rows 183–189.
- decided: apt channel = GitHub Pages self-hosted (→ decisions.md); PR #70 cache TTL-only
  invalidation (recorded in decisions.md on its branch).
- next: Raja merges PR #70; then ticket 14 (release workflow) — Opus 4.8 high implements, Sol high
  first review (last budgeted slot). Driver docs go to main via temp worktree, never feature branches.

## 10:25 — Tickets 14 + 16 merged; PR #70 conflict-resolved and merged; first-tag path clear
- did: Resolved PR #70's merge conflict vs main in a temp worktree (union of two decisions.md entries) →
  CI green → Raja merged. Ticket 14: Opus impl (PR #71), Sol r1 high (last cladex slot: 3 major / 2 minor),
  Opus fix round, fresh-Opus re-review (all 5 verified; 1 MAJOR false positive — `makepkg --needed` IS
  valid per man page, driver rejected), final polish round, driver delta-verify → MERGED (0ebff3c).
  Ticket 16 (GH #65; local ticket file never existed — dispatched from issue body): Opus impl (PR #72),
  Opus r1 empirically reproduced the RPM %license basename-flattening/LICENSE-collision major, fix round
  (%license directory form), driver delta-verify → MERGED (a68e0c9).
- changed: .github/workflows/{release.yml,apt-refresh.yml}, packaging/ci/* (5 scripts),
  packaging/RELEASING.md, packaging/apt/{make-apt-repo.sh --refresh,apt-e2e.sh}, packaging/deb/ring tree +
  copyright, packaging/voisu.spec, packaging/rpm-lib.sh, crates/voisu-app (PR #70), docs (this checkpoint).
- decided: releases tagged from main only (ancestry-gated); smoke installs local artifacts via real
  package-manager paths; equal-pkgver AUR publish = designed no-op → decisions.md.
- next: ticket 15 (live desktop validation, HITL) + Raja's first-tag prerequisites (AUR_SSH_PRIVATE_KEY,
  gh-pages/Pages setup, COPR config, first AUR push, RPM upgrade). Sol budget 0 — all reviews Opus.

## 14:10 — v0.1.0 RELEASED on all four channels; landing page live; three blocker PRs
- did: All first-tag prerequisites closed and v0.1.0 shipped. (1) Verified AUR key/secrets already
  existed (07-19; STATE was stale). (2) Seeded gh-pages, found Pages auto-enabled with a STALE
  raja-dev.me custom-domain claim on Anuraj-dev.github.io (portfolio actually on Vercel → all project
  pages 404'd); removed the claim per Raja (Option B) → canonical URL anuraj-dev.github.io/voisu.
  (3) Landing page built (Opus agent, raja-design) — v1 REJECTED by Raja (clutter/AI slop), v2 minimal
  distro-tab rewrite approved + later fixed (Fedora voisu-overlay install line, voisu setup step);
  README rewritten. (4) COPR custom package created via copr-cli (token in ~/.config/copr, 600);
  COPR_WEBHOOK_URL secret was the `<PACKAGE_NAME>` placeholder → replaced from Integrations page.
  (5) Tag v0.1.0: attempt 1 failed smoke (no --version + lifecycle flakes) → PR #73 (flags TDD +
  6-test deflake, taskset-reproduced, Opus review APPROVE) → attempt 2 failed fedora leg (zombie:
  non-reaping container PID 1; podman-reproduced; daemon PDEATHSIG verified correct) → PR #74 (Opus
  review APPROVE) → attempt 3: ALL SMOKE GREEN, apt + GH Release published; AUR publish rejected
  (tarball blob >488KiB) → PR #75 (strip before git add, driver-verified 9-line) + FIRST AUR PUSH run
  locally in podman arch container (voisu + voisu-bin live). Raja end-to-end tested: dnf remove →
  install from COPR via the page → works, incl. overlay ("mesmerising").
- changed: gh-pages index.html (new + 2 revisions), README.md, crates/voisu-app/bin/* + tests (PR #73),
  tests/daemon_cli_lifecycle.rs (PRs #73/#74), packaging/ci/aur-publish.sh (PR #75), COPR project
  config (external), GH secrets (COPR_WEBHOOK_URL), Anuraj-dev.github.io Pages settings (cname removed).
- decided: canonical URL anuraj-dev.github.io/voisu (no CNAME); AUR repos metadata-only; deflake =
  root-cause + bounded polling, never sleep-padding → decisions.md.
- next: (a) service_cli deflake IN FLIGHT (resumed flake agent; managed_service_lifecycle readiness
  test + family sweep) → verify/PR/merge; (b) re-poke COPR rebuild (cosmetic); (c) scripted apt smoke;
  (d) ticket 15 systematic desktop validation; (e) two parked Raja questions; (f) benchmark rows for
  this session's ~7 dispatches still to log.

## 14:55 — Global Shortcuts re-prompt bug fixed (PR #76 merged)
- did: Raja filed a root-caused bug report (KDE Trigger-Key dialog on EVERY daemon start + 31 leaked
  `[token_voisu_session_<pid>]` sections in kglobalshortcutsrc; live-reproduced). Dispatched Opus 4.8
  (high) coder → constant `"voisu_session"` session token via testable `shortcut_bind_tokens()` seam
  (create/bind request tokens stay PID-unique), regression test, `packaging/voisu.desktop` shipped by
  RPM spec + cargo-deb assets + both AUR PKGBUILDs, manual-cleanup troubleshooting in
  packaging-fedora.md. Coder CAUGHT a report gap: voisu-bin installs from the release tarball → driver
  added the release.yml install line (classifier blocked workflow commit; Raja committed via `!`).
  Opus 4.8 (high) review: APPROVE-WITH-NITS — verified delivery-session token correctly untouched,
  concurrency-safe (escaped_sender in path), cleanup grep spares the new live section; nits fixed
  (softened "stable app_id" comments; watch first-.desktop lintian at next release). CI 3/3 green ×2
  (re-ran after release.yml commit; "Base branch was modified" merge bounce = GitHub race, retry worked).
- changed: crates/voisu-app/src/system.rs (+test), packaging/voisu.desktop (new), voisu.spec,
  crates/voisu-app/Cargo.toml (deb assets), packaging/aur/{voisu,voisu-bin}/PKGBUILD,
  .github/workflows/release.yml, docs/packaging-fedora.md, docs/model-benchmark.md (rows 196-197).
- decided: session_handle_token constant, never per-process (spec-valid; portal-kde persists component
  by that name) → decisions.md. No auto-pruning of user kglobalshortcutsrc — docs-only cleanup.
- next: rebuild (COPR re-poke) + reinstall + §8 verify (one final prompt expected, then silence);
  service_cli deflake agent still stress-running; SIGKILL sender at 14:13 unidentified (trigger only,
  not cause — auditctl rule documented in the bug report if it recurs).

## 16:35 — First-run gaps fixed from live Arch/Hyprland field testing (PR #77 merged)
- did: Raja's friend fresh-installed on Arch/Hyprland and hit: (1) voisu.service dead pre-exec,
  226/NAMESPACE — ReadWritePaths=%h/... under ProtectSystem=strict fails when ~/.config/voisu doesn't
  exist (fresh homes only; Fedora worked by accident); (2) no shortcut dialog on Hyprland (by design —
  bind goes in hyprland.conf); (3) Type Delivery dead on Hyprland (XDPH has no RemoteDesktop portal).
  PR #77 (Opus coder + Opus review, APPROVE-WITH-NITS): ConfigurationDirectory/StateDirectory=voisu +
  ReadWritePaths=%t (systemd provisions dirs pre-sandbox), 5 smoke guards, doctor GlobalShortcuts
  probe + failed-unit→journalctl, README Trigger-Key-by-desktop + clipboard-on-Hyprland, issue
  template. Review caught: hardcoded `voisu:voisu-toggle` bind app-id half unverifiable → made
  self-verifying via `hyprctl globalshortcuts`. Also: COPR rebuild mystery solved — make-srpm.sh
  builds highest v-tag by design, so rebuilds never pick up main; fixes ship only via v0.1.1.
  CI flaked once on managed_service_lifecycle (the exact in-flight deflake target; on an UNcontended
  GH runner — good datapoint), rerun green. Benchmark rows 198-199.
- changed: packaging/voisu.service, packaging/ci/smoke-*.sh ×4, packaging/fedora-smoke.sh,
  system.rs (doctor), service.rs, daemon_cli_lifecycle.rs, README.md, .github/ISSUE_TEMPLATE/.
- decided: per-user dirs are provisioned at service start (systemd directives), never at package
  install (packages can't touch $HOME) → decisions.md. Hyprland bind string must be read from
  hyprctl, never hardcoded. Hyprland Type Delivery = parked product question (c).
- next: Raja tags v0.1.1; then §8 verify on Fedora + friend re-tests Arch fresh-run; deflake agent
  still in flight; earlier release-day benchmark rows still unlogged.
