# Sessions — 2026-07-16

## 01:25 — Ticket 04 process-lifetime follow-up
- did: added Linux capture parent-death signaling, test daemon process-group cleanup, bounded/trapped fake stubs, and failed-Deepgram-chunk cleanup before Idle.
- changed: `crates/voisu-app/src/system.rs`, `crates/voisu-app/tests/daemon_cli_lifecycle.rs`, and `docs/STATE.md`.
- decided: no provider/deadline semantics changed; completion-error cleanup stays inside the Deepgram stream owner.
- next: run the full and repeated parallel acceptance gates outside the managed sandbox, then Sol re-review and commit through the orchestrator.

## 02:16 — Ticket 04 approved and closed
- did: ran Sol re-review round 4 to APPROVE; closed issue #4. CI workflow (`.github/workflows/ci.yml`, 36a3bc6)
  now gates every push/PR with the workspace suite plus a 3x-parallel voisu-app flake check, green on all commits.
  Filed follow-up issue #14 for cancellation-safe `DeepgramStream::abort`/`GroqStream::abort` (drain→peek-then-pop)
  plus an abort-deadline regression test.
- changed: `docs/model-benchmark.md` (rows 15-22 for ticket 04 dispatches/reviews), `docs/STATE.md`.
- decided: Opus 4.8 subagents (medium/high) become the default workhorse for implementation/fix work; Sol is
  reserved for architectural tickets and all code reviews (first review high, re-reviews medium).
- next: dispatch Ticket 05 (reconcile + guard Transcript) — Sol medium implementation, first review Sol high.

## 02:37 — Ticket 05 Transcript decision pipeline
- did: implemented deterministic near-identical selection, bounded configured Groq reconciliation, five quality
  guardrail classes, one bounded repair, clean-source fallback/Quality Failure, and structured decision evidence.
- changed: `voisu-core` decision/error/IPC types and 9 unit tests; daemon controlled/public flow and 6 acceptance
  tests; system Groq reconciliation adapter and processing response budget; `docs/STATE.md` and `docs/decisions.md`.
- decided: candidate decision stays behind the async validator boundary so no intermediate text reaches Delivery;
  cloud reconciliation has a 3s outer deadline and 2s owning curl deadline -> `docs/decisions.md`.
- next: run the full 92-test host gate (managed sandbox denies Unix socket binds), first Sol review, then orchestrator commit.

## 03:30 — Ticket 05 approved and closed
- did: ran host gate (97 tests, CI green on b5c0e29/59b1caa/bcabfef/9769886). Sol first review flagged the
  reconciliation-timeout detach bug (spawn_blocking handle dropped instead of cancel+await) a third time in this
  class; Opus 4.8 fixed it with pinned-future cancel + bounded 1s grace-await, moved Secret Service lookup inside
  the owned spawn_blocking task, promoted `CancelRegistry` into voisu-core, and added token-level Latin/Greek/
  Cyrillic confusable-mixing rejection (later hardened to full Unicode ranges, then Latin Extended-F). Sol re-review
  round 4 APPROVED; issue #5 closed.
- changed: `docs/STATE.md`, `docs/model-benchmark.md` (rows 23-26, ticket 05 dispatches/reviews).
- decided: pin-cancel-bounded-await is now the standing discipline for every cancellable owned future, not just
  process Child handles.
- next: dispatch Ticket 06 (06-correlated-local-diagnostics) — regular feature work, Opus 4.8 high; first review
  Sol high. Issue #14 (Deepgram/Groq abort() drain detach) still open, non-blocking.

## 05:10 — Ticket 06 approved and closed
- did: ran Sol re-review round 5 to APPROVE; closed issue #6 (correlated local diagnostics: DiagnosticStore,
  redacted export, debug-audio expiry, history/export/replay via CLI+IPC, `Replaying` state). Test inventory now
  132 (89 acceptance incl. 1 ignored live smoke + 20 diagnostics + 6 provider-coordination + 14 Transcript-decision
  + 3 app unit). CI green throughout.
- changed: `docs/STATE.md`, `docs/model-benchmark.md` (rows 27-30, ticket 06 dispatches/reviews).
- decided: replay protocol takes a fixture NAME (not a path) inside the private diagnostics/fixtures dir;
  `DebugAudioRecord.path` renamed to `file_name` to match, closing the path-traversal review finding.
- next: dispatch Ticket 07 (07-global-shortcut-toggle) — Opus 4.8 high implementation, first review Sol high.
  Issue #14 (Deepgram/Groq abort() drain detach) still open, non-blocking.

## 06:18 — Ticket 07 approved and closed
- did: Sol round 3 APPROVED Ticket 07 (issue #7, toggle Recording via the Global Shortcuts portal); closed.
  Delivered across three commits: d593f8a (portal boundary traits, listener, controlled portal, voisu shortcut
  integration — Opus 4.8 high; production zbus client initially deferred fail-closed, which Sol ruled BLOCKING),
  b4f39ba (persistent zbus client + real controlled GlobalShortcuts D-Bus service on private per-test dbus-daemon
  buses; channel-file fake removed; `VOISU_DISABLE_SHORTCUTS=1` keeps non-shortcut tests off the host desktop;
  new zbus 5 dependency recorded in docs/decisions.md), 900f456 (NameOwnerChanged rebind loop via ShortcutEvent
  enum, broad Request.Response subscription, authoritative session_handle adoption, session closed on bind
  failure, mock serves Session.Close). Test inventory now 141 (3 app unit + 98 acceptance incl. 1 ignored live
  smoke + 20 diagnostics + 6 provider-coordination + 14 Transcript-decision). CI green.
- changed: `docs/STATE.md`, `docs/model-benchmark.md` (rows 31-34, ticket 07 dispatches/reviews).
- decided: production D-Bus/portal clients must never ship fail-closed as a placeholder — Sol treats a stubbed
  production path as BLOCKING even when test coverage is solid; per-test private dbus-daemon buses are now the
  standing pattern for exercising real D-Bus services in acceptance tests.
- next: dispatch Ticket 08 (08-libei-delivery, input synthesis via libei/portal RemoteDesktop) — likely
  architectural; evaluate impl at Sol medium per pinned routing, first review Sol high. Issue #14 (Deepgram/Groq
  abort() drain detach) still open, non-blocking.

## 06:48 — Ticket 08 RemoteDesktop/libei Delivery implemented
- did: added clipboard-first Delivery outcomes/evidence, background persistent RemoteDesktop permission setup,
  CreateSession/SelectDevices/Start/ConnectToEIS over zbus, runtime-loaded libei UTF-8 TEXT Delivery with bounded
  pong acknowledgement, explicit fallback for every ticket failure class, and controlled public-interface tests.
- changed: `voisu-core` Delivery outcome/diagnostics types; daemon response/evidence wiring; Fedora clipboard,
  RemoteDesktop, and libei adapters; daemon/CLI and new Delivery acceptance tests; `docs/STATE.md` and
  `docs/decisions.md`.
- decided: clipboard preservation gates direct success; permission setup is background work; native libei TEXT is
  required and missing capability fails closed rather than synthesizing layout-dependent raw keycodes.
- next: run the 148-test host gate, then Sol first review at high effort and fix/re-review until approved; do not
  commit this implementation session.

## 09:30 — Tickets 08 and 09 reviewed, green, and merged
- did: completed Ticket 08 after Sol's first review found native TEXT NUL termination, Fedora libei 1.5
  compatibility, application-acceptance overclaiming, restore-token persistence, and repeated permission-prompt
  defects. Added optional libei 1.6 TEXT plus active-XKB Ctrl+V fallback for 1.5, private rotating restore tokens,
  terminal authorization failures, truthful `compositor_submitted` evidence, and CI's missing xkbcommon package.
  PR #15 passed the exact-head workspace + 3x flake gate, merged at `a5771a9`, and closed issue #8.
- did: implemented Ticket 09's `voisu service install|start|stop|restart|status|uninstall` surface. The manager
  atomically installs a trusted daemon copy, enables one graphical-session unit ordered after D-Bus/PipeWire/portal,
  combines systemd state with daemon IPC state, prevents manual-daemon restart loops, restarts managed upgrades,
  and disables/waits/cleans on uninstall. Eight public CLI acceptance tests cover lifecycle, ownership, duplicate
  races, atomic replacement, failed states, stop deadlines, and cleanup. `systemd-analyze --user verify` passed.
- reviewed: Sol re-review/first-review attempts were blocked by the Codex account usage cap. The self-review fixed a
  graphical-target ordering cycle, root-owned package rejection, misleading failed-state reporting, stop timeout
  false-success, and incompatible manual-daemon edges. The final Sonnet reader produced no output and was stopped,
  per the user's fallback instruction. Full host gate: 160 passed, 1 opt-in live smoke ignored, 0 failed.
- shipped: PR #16 passed exact-head CI, merged at `9b58f99`, and closed issue #9. Current `main` equals
  `origin/main`; next ordered ticket is #10. Follow-up issue #14 remains open.

## 09:35 — Post-merge verification and leftover sweep
- did: re-verified the full gate on `main` at `9b58f99` — workspace suite plus 3x `voisu-app` flake runs all green
  (99+7 per run); exact-head CI green. Swept for Ticket-08 leftovers: `cargo check --workspace --all-targets` is
  clean with zero warnings, so no dead code remains from the abandoned mid-session NativeEiSender rewrite (codex's
  `a5771a9` replaced it wholesale).
- decided: codex dispatch prompts are now self-contained — no "delegation to Claude" blocks (Raja: GPT agents
  prompt Claude poorly and waste tokens). -> decisions.md
- next: Ticket 10 (issue #10): recover cleanly from real workflow failures.

## 10:24 — Ticket 10 workflow recovery implemented
- did: added public-interface recovery coverage for microphone disappearance/reconnection; provider disconnect,
  malformed JSON, quota rejection, and Provider Deadline fallback; portal revocation/restart with clipboard
  Delivery; killed CLI and daemon processes; repeated capture failures; stale socket recovery; owned boundary-
  process reap; and a separate ignored full Fedora recovery smoke. Added a three-start/30-second systemd rate limit.
- changed: daemon controlled failure seam, daemon/CLI and service acceptance tests, user-unit generation, Ticket 10
  checklist, `docs/STATE.md`, and `docs/decisions.md`. Commits: `86b2225`, `d6bd6b0`.
- decided: systemd may restart abrupt daemon failures, but persistent startup failure is rate-limited; failed
  Recordings are never replayed across the exactly-once Delivery boundary. -> decisions.md
- verified: `cargo test --workspace` — 166 passed, 2 ignored, 0 failed; `cargo build --workspace` clean. The ignored
  Fedora recovery smoke was compiled but not run because it requires explicit live desktop/provider opt-in.
- next: first Ticket 10 review, exact-head CI, PR/merge, and issue #10 closure; keep issue #14 out of this ticket.

## 10:53 — Ticket 10 round-1 review findings fixed
- did: centralized guarded parent-death handling for every external child, including provider/clipboard/secret-tool,
  PipeWire, and service-managed systemctl processes; added deterministic signal and parent-race probes; confirmed
  both production call sites turn RED when temporarily reverted. Reworked portal revocation/restart acceptance to
  use production system boundaries on a private bus and assert exact clipboard Transcript bytes. Marked acceptance
  proofs that exercise pre-existing `main` behavior honestly instead of claiming new production algorithms.
- changed: `voisu-app` process/spawn handling, daemon/CLI and service acceptance tests, Ticket 10 status,
  `docs/STATE.md`, `docs/decisions.md`, and this session log. The ignored live recovery smoke now requires an absent
  prior installation, verifies a changed systemd MainPID, and has panic-safe disable/remove cleanup.
- decided: every external child gets one shared `PR_SET_PDEATHSIG=SIGKILL` hook plus a no-allocation PPID race check;
  per-command parent-death hooks are not sufficient. -> decisions.md
- verified: `cargo test --workspace` — 170 passed, 2 ignored, 0 failed; `cargo build --workspace` clean.
- next: Ticket 10 re-review, exact-head CI, PR/merge, and issue #10 closure.

## 11:17 — Issue 14 cancellation-safe provider abort
- did: changed the Deepgram and Groq abort paths to retain each owned task handle until its await completes; added a
  real-adapter coordinator regression covering cancellation at the abort deadline.
- changed: `crates/voisu-app/src/system.rs`; refreshed `docs/STATE.md` and this session log for the checkpoint.
- decided: no new load-bearing decision; this enforces the existing own -> cancel -> bounded await/reap invariant.
- verified: the test was RED with `drain(..)` in each adapter independently and GREEN with await-then-pop;
  `cargo test --workspace` — 171 passed, 2 ignored, 0 failed; `cargo build --workspace` clean.
- next: commit with `fix(providers): retain abort handles through await (#14)` from a checkout where `.git` is
  writable, then review/merge and close issue #14.

## 11:25 — Orchestrator: Ticket 10 merged, #14 fix parked on branch
- did: Ticket 10 full cycle — Sol medium impl, Sol high review (2 BLOCKER/1 HIGH/1 MEDIUM), Sol medium fix round,
  Sol medium re-review APPROVE, PR #17 squash-merged at `aa8055a`, CI green, issue #10 closed. Dispatched issue #14
  to Sol medium; fix delivered and committed `d52c7d7` on `issue-14-cancellation-safe-abort` (pushed), session
  stopped by Raja before review/merge.
- changed: crates/voisu-app (process.rs new, system.rs, service.rs, tests), docs/model-benchmark.md (rows 39–41).
- decided: nothing new beyond what codex logged above.
- next: review/PR/merge issue #14 branch, then Ticket 11 (GTK capsule, Luna).

## 13:17 — Issue 14 round-1 review fixes
- did: closed the abort-deadline drop window by making Deepgram/Groq stream destruction cancel every remaining
  Tokio task and transfer each handle to a reaper until cancellation completes; retained the per-Recording cancel
  signal so already-running curl children are still killed and reaped by their owning bounded waits.
- changed: `crates/voisu-app/src/system.rs`; refreshed `docs/STATE.md` and this session log.
- decided: no new load-bearing decision; this completes the existing own -> cancel -> bounded await/reap invariant.
- verified: the regression now waits boundedly for both gate receivers to close, asserts both sends fail, and then
  observes zero completions across a bounded scheduler window. Reinstating `drain(..)` fails at receiver closure;
  `cargo test --workspace` — 171 passed, 2 ignored, 0 failed; `cargo build --workspace` clean.
- next: commit the focused fix, then Sol medium re-review, PR, exact-head CI, merge, and close issue #14.
## 14:47 — Ticket 11 GTK voice capsule
- did: implemented approved DESIGN.md tokens, presentation state model, observer-only OverlayStatus IPC, and optional GTK4 Layer Shell binary.
- changed: DESIGN.md; voisu-core protocol; voisu-app overlay model, daemon, and binary; Ticket 11 checklist; state/index/decision docs.
- decided: preserve CLI Status compatibility and expose terminal feedback only through OverlayStatus; gate GTK dependencies behind the opt-in overlay feature.
- next: compile `cargo check -p voisu-app --features overlay` and capture Fedora KDE screenshots on the orchestrator/host.
## 15:08 — Ticket 11 review round
- did: Fixed pointer click-through, replaced sticky boolean feedback with typed terminal OverlayEvents, added ID-aware expiring presentation control, and applied DESIGN.md GTK treatments.
- changed: `voisu-core` IPC types, daemon observer publication, `overlay.rs` controller/tests, GTK overlay, and daemon/CLI acceptance coverage.
- decided: Overlay events are observer-only and omitted from normal Status responses; Quality Failure is emitted only when transcript guardrail evidence exists.
- next: On Fedora KDE with GTK development packages, compile `--features overlay` and run the screenshot gate for Recording, Processing, Success, Failure, and Idle.
## 18:36 — Ticket 11 GTK compile fix
- did: adapted the overlay to the installed gtk4-layer-shell 0.8 trait API, enabled GTK4 accessibility APIs, disambiguated the window display, and defaulted unknown reduced motion to true.
- changed: `crates/voisu-app/Cargo.toml`; `crates/voisu-app/src/bin/voisu-overlay.rs`; refreshed `docs/STATE.md`.
- decided: preserve Overlay layer, no keyboard mode, existing geometry/input region/accessibility descriptions, and conservative motion behavior.
- verified: `cargo check -p voisu-app --features overlay` and `cargo test --workspace` passed; live Fedora screenshot gate remains pending.
- next: capture and review the five DESIGN.md states on Fedora KDE Plasma / Wayland.

## 19:12 — Ticket 12 Overlay capability fallback
- did: added a pure, injected-capability feedback selector; the GTK Overlay now uses runtime Layer Shell support,
  regular unfocusable surfaces, or notification feedback with named degradation. Added a separate bounded Overlay
  supervisor and an observer-owned backend report; no daemon path was added.
- changed: `crates/voisu-app/src/feedback.rs`, Overlay adapter/controller tests, Ticket 12 checklist, decision log,
  and project state.
- decided: `voisu status` stays daemon-only; Overlay backend identity/reason belongs to structured Overlay logs and
  `voisu-overlay --report-backend`. -> decisions.md
- verified: RED compiler proofs for the new selection and restart contracts, then GREEN; `cargo test --workspace` —
  194 passed, 2 ignored, 0 failed; `cargo check -p voisu-app --features overlay` and `cargo build --workspace` clean.
- next: review and merge Ticket 12, then start Ticket 13 package verification.

## 19:33 — Ticket 12 round-1 review findings fixed
- did: made no-display feedback a persistent read-only journal observer, removed the impossible in-process missing-GTK claim, added named XWayland fallback selection, retained the GTK notification application with an `ApplicationHoldGuard`, and treat a surface as usable only after a bounded map signal following `present`.
- changed: `crates/voisu-app/src/feedback.rs`, `crates/voisu-app/src/bin/voisu-overlay.rs`, Overlay selector tests, Ticket 12 checklist, and decision log.
- decided: missing dynamically linked GTK is recorded by the launcher/systemd journal because the Overlay executable cannot reach `main`; no-display logging is the genuine notification surface of last resort. -> `docs/decisions.md`
- next: obtain the Ticket 12 re-review, then merge; live compositor behavior still needs Fedora KDE verification beyond headless contract tests.

## 20:40 — Ticket 12 round-2 review findings fixed
- did: replaced the unsound compositor-map surface probe (finding 1) with honest local realization — a present
  `window.surface()` on the first real show is surface creation; only GTK realizing without a surface falls back to a
  desktop notification, and compositor rejection is left to terminate the process where the bounded `--supervise`
  policy handles it, so a false fallback on a healthy compositor is now impossible. Removed the startup `present()` and
  500 ms grace (finding 2): the window stays hidden at Idle with no styled empty-capsule flash and status polling
  starts immediately, so an early Recording is shown at once. Marked the two obsolete decision-log entries superseded
  and recorded the round-2 ruling (finding 3).
- changed: `crates/voisu-app/src/feedback.rs` (honest `after_surface_creation` doc/param + realized-surface contract
  test), `crates/voisu-app/src/overlay.rs` (hidden-at-Idle / immediate-Recording test),
  `crates/voisu-app/src/bin/voisu-overlay.rs` (realize-time surface probe, no startup present, immediate polling with a
  `switched` guard), `docs/decisions.md`, the Ticket 12 checklist, and this log.
- decided: Overlay surface creation is local GTK realization, not a compositor map probe (direction (a)); a compositor
  rejection is an honest process-terminating protocol error bounded by the supervisor. -> `docs/decisions.md`
- verified: `cargo test --workspace` — 198 passed, 2 ignored, 0 failed; `cargo check -p voisu-app --features overlay`
  clean with zero warnings; `cargo build --workspace` clean. Live Fedora KDE compositor verification of the removed
  flash and process-termination path remains beyond headless contract tests.
- next: Ticket 12 re-review, then merge.

## 20:30 — Ticket 13 Fedora RPM release candidate
- did: Added the Fedora RPM spec, packaged graphical-session user unit, exact-commit/Cargo.lock build script, base
  artifact and optional Overlay smoke harness, Fedora packaging procedure, and release-evidence matrix. Updated
  service management so a packaged unit is preferred and stale Ticket 09 XDG user-data ownership is migrated away.
- changed: `packaging/`, `crates/voisu-app/src/service.rs`, `crates/voisu-app/tests/service_cli.rs`,
  `docs/packaging-fedora.md`, `docs/release-evidence.md`, `docs/STATE.md`, `docs/INDEX.md`,
  `docs/decisions.md`, and Ticket 13's checklist.
- decided: one GTK-free base RPM plus an optional `voisu-overlay` subpackage; RPM removal owns packaged artifacts,
  while CLI uninstall disables the packaged service and preserves user data. -> `docs/decisions.md`
- verified: `cargo test --workspace` — 201 passed, 2 ignored, 0 failed; `cargo build --workspace`,
  `cargo check -p voisu-app --features overlay`, shell syntax, and diff checks passed. `rpmbuild` and live Fedora
  acceptance remain pending on the orchestrator host.
- next: Build the exact tested RPM on Fedora, run `packaging/fedora-smoke.sh`, and fill pending release evidence.
## 21:05 — Ticket 13 round-1 review fixes
- did: fixed all confirmed Fedora packaging findings; added Cargo vendor `Source1` and offline RPM builds, exact
  artifact installation/NEVRA/SHA-256 smoke verification, corrected Fedora dependencies, removal ordering, license
  metadata, and canonical full-commit handling.
- changed: `packaging/`, `LICENSE`, `crates/voisu-app/src/service.rs`, `crates/voisu-app/tests/service_cli.rs`,
  `docs/packaging-fedora.md`, `docs/release-evidence.md`, `docs/STATE.md`, and `docs/decisions.md`.
- decided: a packaged user unit is trusted only when its executable and `ExecStart` validate; otherwise install uses
  the Ticket 09 user-data path with an explicit report. RPM removal starts with desktop-user `voisu service uninstall`.
- verified: RED regression failed before validation and GREEN afterward; `cargo test --workspace` — 202 passed, 2
  ignored, 0 failed; `cargo build --workspace`, overlay feature check, `bash -n`, `git diff --check`, and offline
  vendor/archive source replacement construction passed. `rpmbuild` remains unavailable; artifact/live evidence is
  PENDING.
- next: build the exact RPM and run the Fedora host smoke/release-evidence checklist.
## 21:25 — Ticket 13 round-2 review + escalation (orchestrator checkpoint)
- did: Sol MEDIUM round-2 re-review of the Ticket 13 round-1 fixes (`a4e978e`) — 3 HIGH + 1 MEDIUM survived:
  inverted packaged-unit dir precedence + missing effective-unit validation, bypassable smoke artifact binding
  (same-NEVRA pre-install), non-deterministic vendor `Source1` tarball, and smoke mutating user-service state it
  claims to leave untouched. Dispatched an Opus 4.8 fix agent for all four (still running at checkpoint).
- changed: `docs/STATE.md` only (fix agent will change `crates/voisu-app/src/service.rs`, `packaging/*.sh`, tests,
  docs when it commits).
- decided: nothing new load-bearing; round-2 items are corrections to decisions already logged at 21:05.
- next: confirm the Opus fix commit landed after `a4e978e`, rerun gates, Sol MEDIUM round-3; on APPROVE add the
  Ticket 13 benchmark rows, PR → CI → squash-merge → close #13, then the final Sol/Terra/Luna-vs-Opus report.
  Host rpmbuild + live smoke still blocked on Raja installing rpm-build/rpmlint/systemd-rpm-macros.

## 22:10 — Ticket 13 round-2 fixes landed
- did: fixed all four Sol round-2 findings. (1) Packaged-unit detection now resolves the EFFECTIVE unit via
  `systemctl --user show voisu.service -p LoadState -p FragmentPath -p ExecStart`, validates the effective
  ExecStart binary, treats XDG-config units as non-packaged, and falls back to a static `/etc`-before-`/usr/lib`
  search only when systemctl is unavailable. (2) `fedora-smoke.sh` binds to the supplied RPM by comparing full
  `rpm -qp --dump` vs `rpm -q --dump` manifests and refuses a same-NEVRA payload mismatch. (3) `build-rpm.sh`
  vendors from the exact-commit git-archive extraction and archives deterministically (`tar --sort/--owner/--group/
  --numeric-owner/--mtime` + `gzip -n`) with a byte-identical re-archive self-test. (4) `fedora-smoke.sh` snapshots
  and restores the user-service state it mutates in a cleanup trap that fires on failure too.
- changed: `crates/voisu-app/src/service.rs`, `crates/voisu-app/tests/service_cli.rs` (fake `systemctl show` now
  emits realistic `LoadState`/`FragmentPath`/`ExecStart`; +3 discriminating tests), `packaging/build-rpm.sh`,
  `packaging/fedora-smoke.sh`, `docs/packaging-fedora.md`, `docs/release-evidence.md`, `docs/STATE.md`.
- decided: trust the unit systemd would actually run, not a static file at a guessed path — administrator `/etc`
  overrides and drop-ins are authoritative for packaged-unit detection.
- verified: `cargo test --workspace` — 205 passed, 2 ignored, 0 failed; `cargo build --workspace`, overlay feature
  check, and `bash -n` on both packaging scripts clean; deterministic vendor tar/gzip confirmed byte-identical
  headlessly. `rpmbuild` remains unavailable; artifact/live evidence is PENDING the host.
- next: Sol MEDIUM round-3 re-review; on APPROVE add Ticket 13 benchmark rows, PR → CI → squash-merge → close #13.

## 23:30 — Ticket 13 round-3 fixes landed
- did: fixed all 5 Sol round-3 findings on top of 674b93e. (1 HIGH) Reached the real stale-shadow migration: a
  Ticket 09 XDG user unit outranks /etc and /usr/lib, so when `systemctl show` reports the XDG fragment (or can't
  answer) the packaged unit is now detected on disk (`disk_packaged_unit`) and migrated. Made the fake `systemctl
  show` model real precedence (XDG shadows packaged); RED-proved the migration test fails without the disk fallback.
  (2 HIGH) Hardened effective-unit validation: any non-`loaded` LoadState → fallback with reason (new `Unloadable`);
  every ExecStart command binary validated, not just the first (`parse_show_execstart_binaries` collects all
  `path=` tokens across all ExecStart= lines). (3 HIGH) build-rpm.sh self-test now runs an INDEPENDENT `cargo
  vendor` of the same commit and compares byte-for-byte, plus `--mode='u+rw,go=rX'` normalizes modes vs umask drift
  (verified umask-independent headlessly). (4 MEDIUM) fedora-smoke.sh restoration is verified: each failure printed,
  forces non-zero exit when the smoke otherwise passed, handles enabled-runtime, reports unrestorable states. (5
  MEDIUM) replaced the 9 `rpm | grep -q` pipelines with captured-output here-strings to avoid SIGPIPE-141 under
  pipefail.
- changed: `crates/voisu-app/src/service.rs` (effective-unit rewrite: EffectiveLookup{Packaged/Unloadable/
  NoPackagedUnit}, disk_packaged_unit, multi-exec + unit-file parsers; dropped the old validate_packaged_unit/
  packaged_daemon_path/PACKAGED_DAEMON_PATH), `crates/voisu-app/tests/service_cli.rs` (realistic fake show; override
  helpers; +2 net tests), `packaging/build-rpm.sh`, `packaging/fedora-smoke.sh`, `docs/packaging-fedora.md`,
  `docs/release-evidence.md`, `docs/STATE.md`.
- decided: trust the unit systemd would actually run, but detect a shadowed packaged unit on disk (systemd
  precedence puts XDG config above packaged dirs); packaging restoration must be verified, never silent.
- verified: `cargo test --workspace` — 207 passed, 2 ignored, 0 failed; `cargo build --workspace` (no warnings),
  overlay feature check, `bash -n` on both scripts, and `git diff --check` clean; deterministic vendor archiving
  confirmed byte-identical across umasks; shadow-migration test RED-proven. `rpmbuild`/live Fedora evidence PENDING.
- next: Sol MEDIUM round-4 re-review; on APPROVE add Ticket 13 benchmark rows, PR → CI → squash-merge → close #13.
