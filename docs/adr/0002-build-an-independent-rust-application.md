# Build an independent Rust application

Voisu will be a new Rust codebase rather than a HyprVox fork or a transitional
TypeScript implementation. HyprVox may inform architectural choices and
adapted MIT-licensed work will retain attribution; the cost of porting is
accepted in exchange for native Linux integration, low runtime overhead, and
maintainable RPM and DEB packaging.

