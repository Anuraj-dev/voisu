#[ignore = "L0-DEFECT: portal Trigger Key uniqueness is not enforced"]
#[test]
fn portal_bind_fails_closed_when_the_trigger_key_chord_is_already_occupied() {
    unimplemented!("FedoraShortcutPortal::bind must reject an occupied chord");
}
